#!/usr/bin/env bash
# Mock Homebrew — controlled by MOCK_BREW_FAIL env var

if [[ "${MOCK_BREW_FAIL:-false}" == "true" ]]; then
  echo "${MOCK_BREW_FAIL_MSG:-Error: brew install failed}" >&2
  exit 1
fi

echo "==> Installing ${2:-pkg}..."
echo "✓ ${2:-pkg} installed"
