#!/usr/bin/env bash
# Mock apt-get — controlled by MOCK_APT_FAIL env var

if [[ "${MOCK_APT_FAIL:-false}" == "true" ]]; then
  echo "${MOCK_APT_FAIL_MSG:-E: Unable to locate package}" >&2
  exit 100
fi

echo "Reading package lists... Done"
echo "Setting up ${*: -1}..."
