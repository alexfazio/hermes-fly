1. LOW: stderr contract is not actually asserted in the warning test
   In tests/deploy.bats:1697, the test uses deploy_resolve_hermes_ref 2>&1 and then checks merged $output. That confirms
   warning text exists, but not that it is on stderr.
   Actionable fix:
   1. Switch to run --separate-stderr ... for the override-path test.
   2. Assert $stderr contains non-reproducible build.
   3. Assert $output is only the resolved ref value.
   4. Apply the same pattern to the default-path companion test to assert empty/no-warning stderr.

   Detailed implementer instructions:
   1. Edit the override warning test in tests/deploy.bats (current block starts at deploy_resolve_hermes_ref warns on non-default override).
   2. Replace `run bash -c '... deploy_resolve_hermes_ref 2>&1'` with `run --separate-stderr bash -c '... deploy_resolve_hermes_ref'`.
   3. Keep the same command body (source ui.sh + deploy.sh, export HERMES_AGENT_REF), but do not merge streams inside the command.
   4. Add/keep success check:
      - `assert_success`
   5. Add stdout assertions that validate function return payload only:
      - `assert_output --partial "custom-branch"` (or current override value)
      - `refute_output --partial "non-reproducible build"` (warning text must not leak to stdout)
   6. Add stderr assertions that validate warning channel:
      - `[[ "$stderr" == *"non-reproducible build"* ]]`
      - `[[ "$stderr" == *"custom-branch"* ]]`
   7. Edit the companion default-path test (deploy_resolve_hermes_ref default path does not emit warning) to use the same `run --separate-stderr` pattern.
   8. In that default-path test, assert:
      - stdout contains only the resolved default ref value shape
      - stderr does not include warning text (ideally stderr is empty)
   9. Keep existing semantic checks already in suite:
      - default ref remains 40-char lowercase hex
      - override path still returns override value on stdout

Validation criteria (implementer sign-off checklist):
1. Stream-separation correctness:
   1. Override warning test uses `run --separate-stderr` (no `2>&1` in command body).
   2. Default-path companion test uses `run --separate-stderr` (no `2>&1` in command body).
2. Override-path behavioral assertions:
   1. Exit code is 0 (`assert_success`).
   2. `$output` includes the resolved override ref string.
   3. `$output` does not include warning phrase `non-reproducible build`.
   4. `$stderr` includes warning phrase `non-reproducible build`.
   5. `$stderr` includes the override ref value (ensures warning message is the one generated by deploy_resolve_hermes_ref).
3. Default-path behavioral assertions:
   1. Exit code is 0 (`assert_success`).
   2. `$output` matches default pinned-ref contract (40-char lowercase hex or equivalent existing project matcher).
   3. `$stderr` does not include `non-reproducible build`.
   4. Prefer strict empty check for stderr when feasible in the test environment.
4. Regression-safety checks (static review of tests):
   1. Existing test `HERMES_AGENT_DEFAULT_REF is a 40-char lowercase hex SHA (not main)` remains present and unchanged in intent.
   2. Existing override-resolution test still confirms override value is returned by function.
   3. No test weakens previous guarantees by switching from exact/shape checks to broad substring-only checks where shape validation existed.
5. Scope discipline:
   1. Only review-target files are modified (primarily tests/deploy.bats; no unrelated production behavior changes required for this finding).
   2. No behavior changes to deploy_resolve_hermes_ref implementation are introduced to satisfy this test-only finding.
6. Reviewer-ready evidence in PR description or implementer notes:
   1. Explicitly state that stderr contract is now asserted with separated streams.
   2. Mention both paths were validated: override path warns on stderr; default path is warning-free.
